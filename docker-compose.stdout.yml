# ─────────────────────────────────────────────────────────────────────────────
# 오버라이드: api 의 로깅 드라이버를 fluentd → json-file 로 되돌린다.
#
# 쓰는 곳은 두 군데다.
#
# 1) 수집기 없이 앱만 띄워 "앱이 한 줄짜리 JSON 을 제대로 뱉는가"만 확인하고 싶을 때.
#
# 2) Phase 6-A 검증 — "컨테이너가 죽으면 로그도 같이 죽는다"를 보여줄 때.
#    이 모드로 로그를 쌓고 `docker compose rm -sf api` 하면 그 로그는 흔적도 없이 사라진다.
#    같은 시각의 로그가 Dashboards 에는 그대로 남아 있는 화면과 나란히 놓으면,
#    "로그 시스템이 왜 필요한가"에 대한 설명이 한 장으로 끝난다.
#
# ※ 실측 정정: fluentd 드라이버를 써도 `docker compose logs api` 는 읽힌다.
#   Docker 20.10 부터 dual logging 이라 해서, 원격 드라이버를 쓸 때도 로컬에
#   링버퍼 캐시를 함께 남기기 때문이다. 다만 그 캐시는 컨테이너에 붙어 있어서
#   컨테이너가 사라지면 같이 사라진다 — 위 2)의 대비는 그대로 성립한다.
#
# 사용:
#   docker compose -f docker-compose.yml -f docker-compose.stdout.yml up -d api
#   docker compose logs -f api
#
# 원래대로 되돌리기:
#   docker compose up -d --force-recreate api
# ─────────────────────────────────────────────────────────────────────────────

services:
  api:
    logging:
      driver: json-file
      options:
        max-size: "10m"
        max-file: "3"
